# Changelog: August 11, 2026

## Author: Srinjoy Mukherjee

### 1. PyTorch Code Generation & Editor State
- **Code Generation Logic**: Implemented PyTorch code generation logic to parse graph structures into executable models.
- **Zustand Store**: Added Zustand store for centralized editor state management.

### 2. Tooltips & Documentation Updates
- **Tooltip Feature**: Introduced tooltips for UI elements.
- **Documentation**: Refreshed general documentation.

### 3. Comprehensive Backend Testing Suite
- **Block Inference Validation**: Created `test_blocks.py` covering boundary cases for `LinearBlock`, `Conv2DBlock`, and `InputBlock`. Fixed a bug in `Conv2DBlock` where it permitted negative spatial dimensions.
- **Compiler & Execution Tests**: Created `test_compiler.py` to assert correct topological sorting, detect cyclic loops, and validate dynamically generated PyTorch AST syntax.
- **API Integration Tests**: Updated `test_api.py` to cover `/api/blocks` and successful/failed requests to `/api/compile`.

---

## Author: Sampad De

### 1. Testing Strategy and Design
- **Rigorous Testing Strategy Plan**: Designed and created a comprehensive testing strategy plan in `docs/testing_plan.md` outlining the architecture for backend (PyTest) and frontend (Jest/Playwright) tests, dynamic shape propagation checks, compiler AST/runtime validations, and CI/CD automation pipelines.

### 2. Documentation and Configuration
- **Documentation Restructuring**: Renamed `docs/roadmap_and_features.md` to `docs/TODO.md` to improve clarity.
- **Documentation Index**: Updated `docs/index.md` to link to the new `docs/TODO.md` and `docs/testing_plan.md`, and added today's changelog entry.
- **TypeScript & Compiler Config**: Updated `tsconfig.json` to compile JSX to `react-jsx` instead of `preserve`, and updated Next.js environment types in `next-env.d.ts`.
