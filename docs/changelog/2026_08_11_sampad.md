# Changelog: August 11, 2026
**Author**: Sampad De

## 1. Testing Framework and Test Suite
- **FastAPI Endpoint Tests**: Added comprehensive API integration tests in `backend/tests/test_api.py` covering `/api/blocks`, `/api/blocks/{block_id}/docs`, `/api/compile` (valid graphing and cycle detection), and `/api/check` (shape checking and shape mismatches).
- **Tensor Operations & Shape Inference Tests**: Added unit tests in `backend/tests/test_tensor_ops.py` for verifying dimension broadcasting logic and shape inference across `AddBlock`, `SubBlock`, `MulBlock`, `DivBlock`, and `MatMulBlock`.
- **Test Configuration**: Created `backend/tests/conftest.py` to ensure the backend directory is in the Python search path.
- **Dependencies**: Added `pytest` and `httpx` to `backend/requirements.txt` for local testing.

## 2. Documentation and Configuration
- **Documentation Restructuring**: Renamed `docs/roadmap_and_features.md` to `docs/TODO.md` to improve clarity.
- **TypeScript & Compiler Config**: Updated `tsconfig.json` to compile JSX to `react-jsx` instead of `preserve`, and updated Next.js environment types in `next-env.d.ts`.
